switch-key.sh
__pycache__/
*.pyc
*.pyo
.DS_Store
*.egg-info/
dist/
build/
.pytest_cache/
.env

# ClawHub local skill cache — not tracked, for analysis only
clawhub/

# ClawHub staging area — normalized but not yet reviewed/promoted
skill-packages/staging/

# Runtime skill-sync state file (written by install.sh to SKILL_DIR, never to repo)
skill-packages/.bundled-skill-state.json
